{
  "$schema": "https://schema.tauri.app/config/2",
  "bundle": {
    "targets": ["deb", "rpm", "appimage"],
    "linux": {
      "deb": {
        "desktopTemplate": "linux/bulletmd.desktop"
      },
      "rpm": {
        "desktopTemplate": "linux/bulletmd.desktop"
      },
      "appimage": {
        "files": {
          "/usr/share/applications/bulletmd.desktop": "linux/bulletmd.desktop"
        }
      }
    }
  }
}
